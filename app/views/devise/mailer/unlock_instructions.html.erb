<div class="container-fluid offset-4 col-4 mt-5">
    <p class="text-center h4">Hello <%= @resource.email %>!</p>

    <p class="text-center h4">Your account has been locked due to an excessive number of unsuccessful sign in attempts.</p>

    <p class="text-center h4">Click the link below to unlock your account:</p>

    <p><%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token) , class: "btn btn-primary h4"%></p>
</div>